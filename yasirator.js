class ysrator {
    constructor() {}
    search(content) {
        var xhr = new XMLHttpRequest();
        xhr.open("GET", "https://ysrator.github.io/ServerLine/ServerLine.txt");
        xhr.send();
        
    }
}
